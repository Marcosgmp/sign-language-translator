namespace Domain.Enums;

public enum ContractType { CLT, CNPJ }

public enum EmployeeRole { Cashier, Stocker, Manager, Deliverer }

public enum PaymentMethod { CreditCard, DebitCard, Cash }

public enum SaleUnit { Unit, Kilo }
